# Build output
target/
**/target/
build.log
**/build.log

# Local scratch projects (each is its own git repo, pushed to a separate remote)
projects/

# Tooling / downloads
tools/

# IDE
.idea/
*.iml
.vscode/

# OS
Thumbs.db
.DS_Store

# Maven wrapper distribution download marker (keep wrapper scripts + properties)
!**/.mvn/wrapper/maven-wrapper.properties

# native-image build log
native.log
**/native.log

# maven-shade leftover
dependency-reduced-pom.xml
**/dependency-reduced-pom.xml

# Stray build outputs
out/

# Claude Code local config (personal, not tracked)
.claude/scheduled_tasks.lock
.claude/settings.json
.claude/settings.local.json
